<!DOCTYPE html>
<html>
  <head>
    <title>Progresso Fit 💪</title>
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <!--bootstrap-->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-Zenh87qX5JnK2Jl0vWa8Ck2rdkQ2Bzep5IDxbcnCeuOxjzrPF/et3URy9Bv1WTRi" crossorigin="anonymous">
    <!--google fonts-->
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Montserrat:wght@300;700;900&family=Ubuntu&display=swap" rel="stylesheet">
    <!-- font awesome -->
    <script src="https://kit.fontawesome.com/3612fb16df.js" crossorigin="anonymous"></script>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_include_tag "application", "data-turbo-track": "reload", defer: true %>

    <link
      rel="stylesheet"
      href="https://cdnjs.cloudflare.com/ajax/libs/animate.css/4.1.1/animate.min.css"
    />

  </head>

  <body>
    <% unless (action_name == "registration/new" && "sessions/new" && "weights/index") || (controller_name == "weights" || "profiles") %>
      <%= render "shared/navbar" %>
    <% end %>
    <%= render "shared/flashes" %>
    <% if (controller_name == "weights" || controller_name == "profiles") %>
      <%= render "shared/navbar_weights" %>
    <% end %>

    <% if controller_name == "pages" || (action_name == "edit" && controller_name == "registrations") %>
      <%= render "shared/navbar" %>
    <% end %>

    <%= yield %>
  </body>
  <%# <% unless action_name == "registration/new" && "sessions/new" %>
  <%# <%= render "shared/footer" %>
  <%# <% end %>
</html>
